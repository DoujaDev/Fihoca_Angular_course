import { TrimestradorPipe } from './trimestrador.pipe';

describe('TrimestradorPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimestradorPipe();
    expect(pipe).toBeTruthy();
  });
});
